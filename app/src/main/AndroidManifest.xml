<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancetrackingsyatem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.AboutSubject"></activity>
        <activity android:name=".Admin.AboutClass" />
        <activity android:name=".Admin.AboutTeacher" />
        <activity android:name=".Admin.AboutAdmin" />
        <activity android:name=".Admin.Subject" />
        <activity android:name=".Admin.Class" />
        <activity android:name=".Admin.Teacher" />
        <activity android:name=".Admin.Admin" />
        <activity android:name=".Teacher.TeacherHome" />
        <activity android:name=".Admin.AdminHome" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>